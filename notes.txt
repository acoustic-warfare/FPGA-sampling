
För att köra .activate.sh automatiskt och fixa zsh

kopiera över .zshrc

ladda ner aactivator från github

om den säger cowardly refucing kör då:
chmod -R og-w $HOME





Bra för python:

sudo apt install wget build-essential libncursesw5-dev libssl-dev \
libsqlite3-dev tk-dev libgdbm-dev libc6-dev libbz2-dev libffi-dev zlib1g-dev

sudo add-apt-repository ppa:deadsnakes/ppa

sudo apt install python3.11


sudo apt install python3.11-venv

python3 -m venv venv 



sudo apt install pipx

pipx ensurepath




ifall Vivado inte vill ladda ner (fastnar på intalled device list)
    sudo apt update
    sudo apt upgrade
    sudo apt install libncurses5
    sudo apt install libtinfo5
    sudo apt install libncurses5-dev libncursesw5-dev
    sudo apt install ncurses-compat-libs
    
Instal boardfiles till vivado!
Finns länk på gammla wikin, måste göra chmod 777 på lite saker

Installera också cabledrives! 
Finns här med på gammla wikin och gick utan problem




GHDL



VUnit



GTKwave


