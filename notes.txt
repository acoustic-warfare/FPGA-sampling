
För att köra .activate.sh automatiskt och fixa zsh

kopiera över .zshrc

ladda ner aactivator från github

om den säger cowardly refucing kör då:
chmod -R og-w $HOME





Bra för python:

sudo apt install wget build-essential libncursesw5-dev libssl-dev \
libsqlite3-dev tk-dev libgdbm-dev libc6-dev libbz2-dev libffi-dev zlib1g-dev

sudo add-apt-repository ppa:deadsnakes/ppa

sudo apt install python3.11


sudo apt install python3.11-venv

python3 -m venv venv 



sudo apt install pipx

pipx ensurepath

För att installera pip i venv ex. 
./venv/bin/pip install numpy



ifall Vivado inte vill ladda ner (fastnar på intalled device list)
    sudo apt update
    sudo apt upgrade
    sudo apt install libncurses5
    sudo apt install libtinfo5
    sudo apt install libncurses5-dev libncursesw5-dev
    sudo apt install ncurses-compat-libs
    
Instal boardfiles till vivado!
Finns länk på gammla wikin, måste göra chmod 777 på lite saker

Installera också cabledrives! 
Finns här med på gammla wikin och gick utan problem




GHDL
vet inte rikigt hur det löste sig men detta verkar fungera:
Ladda ner GHDL 2.0.0 zip
mkdir ghdl-objs
cd ghdl-objs
./configure --prefix=/usr/local
/home/toad/Downloads/ghdl-2.0.0/configure --disable-werro
sudo chmod 777 -R /usr/local/include/ghdl    
make (detta ska vara i ghdl-objs)
make install 



VUnit
Prova ta bort venv och installera om den med GHDL
Ladda ner VUnit från source:
git clone --recurse-submodules https://github.com/VUnit/vunit.git
python setup.py install (i rätt mapp)

prova även lägga till raderna i run.py (verkar inte behövas):
import sys
sys.path.append("/home/toad/vunit/")


GTKwave
i setting.jason för user lägg till:
 "terminal.integrated.env.linux": {
        "GTK_PATH": ""
   }




   problem med autonegotiation/phy att det bli 1:0 i linkspeed
   klicka på biblioteksfoldern till echo_filerna
   Klicka på system.mss
   klicka på modify this BSP's design
   under temac_adapter_options
   ändra phy_link_speed till 1000Mbps